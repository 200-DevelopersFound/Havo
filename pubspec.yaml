name: learning_digital_ink_recognition_example
description: Demonstrates how to use the learning_digital_ink_recognition plugin.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^5.0.0
  learning_input_image: ^0.0.5
  learning_digital_ink_recognition: ^0.0.1
  flutter_circle_color_picker: ^0.3.0
  flutter_tts: ^3.3.3
  cupertino_icons: ^1.0.4
  loading_indicator: ^3.0.2
  progress_loading_button: ^1.1.4
  google_fonts: ^2.1.1
  google_nav_bar: ^5.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
